name: Deploy to GoDaddy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to GoDaddy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.MYPORTAL_FTP_SERVER }}
          username: ${{ secrets.MY_PORTAL_FTP_USERNAME }}
          password: ${{ secrets.MY_PORTAL_FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./
